# [对比学习LangChain和MAF-15]压缩对话历史

为解决LLM调用无状态的问题，我们不得不将消息历史作为上下文的一部分发送给LLM。但随着对话的进行，消息历史会不断增长，最终可能会超出LLM的上下文窗口限制。随着LLM的上下文窗口的不断扩大，可以有朝一日上下文窗口不再成为问题，但也并不意味着将整个对话历史原封不动地发送给LLM就是最优的选择。LLM拥有上下文的丰富程度与推理的质量并不是完全正相关的，当对话历史的长度超过一定程度后，过长的上下文反而会干扰LLM对当前对话的理解和推理，导致LLM生成的响应质量下降。所以对话历史的压缩需求可能永远都不会消失。我们来看看LangChain和MAF是如何解决这个问题的。

## 1. LangChain

压缩对话历史可以采用不同的策略，最简单的方式是只保留最近的几条消息，或者只保留最近的几轮对话。但是为了尽可能保留对话历史的语境信息，我们一般会在保留最近N论对话的基础上，利用LLM对之前的对话进行摘要压缩。LangChain提供了如下两个中间件来实现对话历史的压缩：

- `SummarizationMiddleware` (langchain.agents.middleware)
- `deepagents.middleware.summarization._DeepAgentsSummarizationMiddleware`

由于`SummarizationMiddleware`又是`_DeepAgentsSummarizationMiddleware`的别名，所以两者都可以叫做`SummarizationMiddleware`。我们可以将它们分别称为LangChain版和DeepAgents版的`SummarizationMiddleware`，后者其实时对前者的进一步包装。

在如下这段演示程序中，我们创建了一个使用`gpt-5.2-chat`的`ChatOpenAI`，然后调用`create_agent`工厂函数创建了一个Agent。我们为它注册了两个中间件，一个是DeepAgents版的`SummarizationMiddleware`，另一个是自定义的`check_summary`中间件。前者用来压缩对话历史，后者用来查看压缩后的结果。

```python
from langchain.agents import create_agent
from langchain.agents.middleware import wrap_model_call
from deepagents.middleware.summarization import SummarizationMiddleware
from deepagents.backends import FilesystemBackend
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage,AIMessage,AnyMessage
from dotenv import load_dotenv
import asyncio

load_dotenv()

@wrap_model_call # type: ignore
async def check_summary(request,handler) :
    for message in request.messages:
        message.pretty_print()
    return await handler(request) # type: ignore

llm = ChatOpenAI(model="gpt-5.2-chat")
summarization_middleware = SummarizationMiddleware(
    model=llm, 
    backend=FilesystemBackend(virtual_mode=True), 
    trigger=("messages", 6), 
    keep=("messages", 3))
agent = create_agent(
    model=llm,
    middleware=[summarization_middleware, check_summary]) # type: ignore

messages:list[AnyMessage] = [
    HumanMessage(content="今天苏州的天气怎么样？"), 
    AIMessage(content="苏州今天是晴天。"),
    HumanMessage(content="气温多少？"),
    AIMessage(content="室外温度25度。"),
    HumanMessage(content="有风吗？"),
    AIMessage(content="西北风4级。"),
    HumanMessage(content="根据天气，给我一些着装建议。")]

async def main():
    result = await agent.ainvoke(input={"messages": messages})  # type: ignore
    print(result["messages"][-1].content)

asyncio.run(main())
```

我们对注册的`SummarizationMiddleware`做了如下的设置：

- **model**：指定了用于摘要压缩的LLM。
- **backend**：指定了用于存储被压缩的消息的后端，这里我们使用了虚拟模式的`FilesystemBackend`，它不会将被压缩的消息写入磁盘，而是直接丢弃。
- **trigger**：指定了触发压缩的条件，这里我们设置为当消息列表长度超过6条时触发压缩。
- **keep**：指定了在压缩后保留的消息数量，这里我们设置为保留最近3条消息。

我们创建了一个包含**7条**消息的列表模拟了一段对话，并将其作为参数传递给Agent的`ainvoke`方法。如下两端输出分别表示压缩后的对话历史和LLM的最终答复。可以看到，压缩后的对话历史只保留了最近**3条**消息，而LLM的最终答复仍然能够正确理解用户的意图并给出合理的建议。

```markdown
================================ Human Message =================================

Here is a summary of the conversation to date:

## SESSION INTENT
User is asking about today’s weather conditions in Suzhou, specifically general weather and temperature.

## SUMMARY
- User asked: “今天苏州的天气怎么样？” (What’s the weather like in Suzhou today?)
- Assistant responded: It is sunny in Suzhou today.
- User then asked: “气温多少？” (What is the temperature?)
- Assistant responded: The outdoor temperature is 25°C.

No additional requests or decisions were made.

## ARTIFACTS
None

## NEXT STEPS
No pending tasks. Await further user queries.
================================ Human Message =================================

有风吗？
================================== Ai Message ==================================

西北风4级。
================================ Human Message =================================

根据天气，给我一些着装建议。
今天苏州**晴天，25°C，西北风4级**，体感会比较舒适，但有一定风力。
```

```markdown
**着装建议：**

- ✅ 上衣：短袖、薄衬衫或透气T恤都可以  
- ✅ 下装：薄款长裤、休闲裤或裙装  
- ✅ 外搭：可以带一件**薄外套或防风外套**（有风时会稍微凉一点）  
- ✅ 鞋子：运动鞋、休闲鞋都合适  
- ✅ 防晒：建议涂防晒霜、戴墨镜或帽子（晴天紫外线较强）

整体来说属于**舒适偏暖型天气**，但风力4级会让早晚或阴凉处稍微凉一点。
```

在我们的文章[利用`SummarizationMiddleware`对长程对话瘦身](https://blog.csdn.net/JaydenAI/article/details/160942433)提供了针对DeepAgents版`SummarizationMiddleware`的更详细的介绍。

## 2. MAF

针对消息的压缩，MAF提供了不止一种解决方案，一种是使用`ReducingChatClient`这个`IChatClient`中间件，另一种就是`CompactionProvider`这个`AIContextProvider`。

### 2.1 ReducingChatClient

如下的程序演示了如何利用`ReducingChatClient`来部分对话内容进行摘要，保证在不丢失基本语义的前提下，腾出更多的上下文窗口。如代码片段所示，我们基于`OpenAIClient`创建了一个`IChatClient`对象，并在此基础上利用`ChatClientBuilder`注册了`ReducingChatClient`中间件，并指定了一个`SummarizingChatReducer`对象来提供基于摘要的队对话精减功能。我们在创建`SummarizingChatReducer`对象的时候，传入了一个用于对摘要进行生成的`ChatClient`对象，该对象依然是基于`OpenAIClient`创建的，并且使用了不同的模型来生成摘要。我们还为`SummarizingChatReducer`对象指定了`targetCount`和`threshold`两个参数，前者表示我们希望在摘要之后保留多少条消息，后者则是一个阈值，用于触发摘要操作的阈值（超过`targetCount+threshold`）。

```csharp
using Azure;
using dotenv.net;
using Microsoft.Extensions.AI;
using OpenAI;

DotEnv.Load();

var apiKey = Environment.GetEnvironmentVariable("API_KEY")!;
var endpoint = Environment.GetEnvironmentVariable("OPENAI_URL")!;

var summaryClient = new OpenAIClient(
    credential: new AzureKeyCredential(apiKey),
    options: new OpenAIClientOptions { Endpoint = new Uri(endpoint) })
    .GetChatClient(model: "gpt-5.2-chat")
    .AsIChatClient();

var client = new OpenAIClient(
    credential: new AzureKeyCredential(apiKey),
    options: new OpenAIClientOptions { Endpoint = new Uri(endpoint) })
    .GetChatClient(model: "DeepSeek-V4-Pro")
    .AsIChatClient()
    .AsBuilder()
    .UseChatReducer(reducer: new SummarizingChatReducer(chatClient: summaryClient, targetCount: 3, threshold: 1))
    .Use((messages, options, next, cancelToken) => {
        var index = 1;
        foreach (var message in messages)
        {
            Console.WriteLine($"{index++}. {message}");
        }
        return next(messages, options, cancelToken);
    })
    .Build();

ChatMessage[] messages = [
    new ChatMessage(ChatRole.User, "今天苏州的天气怎么样？"),
    new ChatMessage(ChatRole.Assistant, "苏州今天是晴天。"),
    new ChatMessage(ChatRole.User, "气温多少？"),
    new ChatMessage(ChatRole.Assistant, "室外温度25度。"),
    new ChatMessage(ChatRole.User, "有风吗？"),
    new ChatMessage(ChatRole.Assistant, "西北风4级。"),
    new ChatMessage(ChatRole.User, "根据天气，给我一些着装建议。")
];

var response = await client.GetResponseAsync(messages);
Console.WriteLine($"\n\n{response}");
```

压缩后的对话历史和LLM的最终答复如下所示。可以看到，压缩后的对话历史只保留了最近3条消息，而LLM的最终答复仍然能够正确理解用户的意图并给出合理的建议。

```markdown
请求消息共计4条
1. 用户询问今天苏州的天气情况，助手回答是晴天。随后用户继续询问气温，助手回复室外温度为25度。对话围绕苏州当天的天气状况展开，涵盖天气类型和具体温度信息。
2. 有风吗？
3. 西北风4级。
4. 根据天气，给我一些着装建议。
```

```markdown
今天苏州晴天，但西北风4级，体感不会太热。建议穿长袖T恤、薄款卫衣或衬衫，外面搭一件防风薄外套。早晚可能会稍凉，早晚出行时注意保暖。
```

我们的文章[ReducingChatClient：精减对话又不丢失语义](https://blog.csdn.net/JaydenAI/article/details/161473702)提供了针对ReducingChatClient的更详细的介绍。

### 2.2 CompactionProvider

`CompactionProvider`支持不同的消息压缩策略，比如摘要、折叠、删除和替换等。由于`CompactionProvider`对压缩策略进行了抽象，原则上我们可以自行提供任一的策略来压缩对话历史。下面的例子演示了针对摘要的压缩策略，它可以在不丢失语义的前提下将多条消息压缩成一条。下面的实例演示了如何利用`CompactionProvider`，结合代表摘要压缩策略的`SummarizationCompactionStrategy`来对对话历史进行摘要压缩。

```csharp
using Azure;
using dotenv.net;
using Microsoft.Agents.AI;
using Microsoft.Agents.AI.Compaction;
using Microsoft.Extensions.AI;
using OpenAI;

DotEnv.Load();

var apiKey = Environment.GetEnvironmentVariable("API_KEY")!;
var endpoint = Environment.GetEnvironmentVariable("OPENAI_URL")!;

var summaryClient = new OpenAIClient(
    credential: new AzureKeyCredential(apiKey),
    options: new OpenAIClientOptions { Endpoint = new Uri(endpoint) })
    .GetChatClient(model: "gpt-5.2-chat")
    .AsIChatClient();

var compactionStrategy = new SummarizationCompactionStrategy(
    chatClient: summaryClient,
    trigger: messageIndex => messageIndex.TotalMessageCount > 6,
    minimumPreservedGroups: 3);
var compactionProvider = new CompactionProvider(compactionStrategy);

var agent = new OpenAIClient(
    credential: new AzureKeyCredential(apiKey),
    options: new OpenAIClientOptions { Endpoint = new Uri(endpoint) })
    .GetChatClient(model: "DeepSeek-V4-Pro")
    .AsIChatClient()
    .AsBuilder()
    .Use((messages, options, next, cancelToken) => {
        var index = 1;
        foreach (var message in messages)
        {
            Console.WriteLine($"{index++}. {message}");
        }
        return next(messages, options, cancelToken);
    })
    .Build()
    .AsAIAgent(options: new ChatClientAgentOptions { AIContextProviders = [compactionProvider] });

ChatMessage[] messages = [
    new ChatMessage(ChatRole.User, "今天苏州的天气怎么样？"),
    new ChatMessage(ChatRole.Assistant, "苏州今天是晴天。"),
    new ChatMessage(ChatRole.User, "气温多少？。"),
    new ChatMessage(ChatRole.Assistant, "室外温度25度。"),
    new ChatMessage(ChatRole.User, "有风吗？"),
    new ChatMessage(ChatRole.Assistant, "西北风4级。"),
    new ChatMessage(ChatRole.User, "根据天气，给我一些着装建议。")
];

var response = await agent.RunAsync(messages);
Console.WriteLine($"\n\n{response}");
```

压缩后的对话历史和LLM的最终答复如下所示。可以看到，压缩后的对话历史同样只保留了最近3条消息，LLM依然能够正确理解用户的意图并给出合理的建议。

```markdown
1. [Summary]
**Conversation Summary**

- User asked about today’s weather in Suzhou.
- Assistant responded: Sunny.
- User asked for the temperature.
- Assistant responded: 25°C.

No tools were used. No additional preferences or context provided.
2. 有风吗？
3. 西北风4级。
4. 根据天气，给我一些着装建议。
```

```markdown
今日苏州晴天，西北风4级，气温25°C，体感稍凉。建议穿长袖T恤或薄衬衫，外搭一件风衣或牛仔外套，防风又方便穿脱。下身可穿长裤，怕凉可以带条薄围巾。
```

我的文章[CompactionProvider：采用多种策略压缩对话历史](https://blog.csdn.net/JaydenAI/article/details/161611720)提供了针对CompactionProvider的更详细的介绍。